[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spatial-collision-engine"
version = "0.1.3"
description = "C++ pybind11 spatial collision engine"
requires-python = ">=3.10"